import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Castle Pharma - Extending Concern in Healthcare",
  description: "Castle Pharmaceuticals - Extending Concern in Healthcare with Ethics, Values & Commitment. Quality pharmaceutical products manufactured to WHO GMP standards.",
  keywords: "pharmaceutical, healthcare, medicines, WHO GMP, Castle Pharma, tablets, capsules, syrups, injectables",
  authors: [{ name: "Castle Pharma" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
